import { makeStyles } from '@mui/styles';

export default makeStyles((theme) => ({
    
      container: {
        padding: theme.spacing(2),
        height: "600px",
      },

      editor: {
        height: "500px",
        resize: "vertical",
      },
  }));