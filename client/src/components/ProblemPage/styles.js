import { makeStyles } from '@mui/styles';

export default makeStyles(() => ({
    container: {
        paddingTop: '20px',
        marginBottom: '85px'
    },

    runButton: {
        margin: 10,
    }

}));

